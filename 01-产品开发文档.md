# ColorFlow 产品开发文档

> **版本**：v0.1 | 日期：2026-05-09 | 状态：规划中  
> **产品**：ColorFlow - AI 矢量输出引擎  
> **定位**：供 AI Agent 调用的高质量矢量描图（位图 → SVG/DXF）封装层

---

## 一、产品概述

### 1.1 产品是什么

ColorFlow 是一个**AI Native 矢量描图封装层**，核心能力是将位图（PNG/JPG/WebP）高质量转换为矢量格式（SVG/DXF）。它不做意图理解、不做参数决策、不做结果校验——只做好"被 AI Agent 调用"这一件事。

**一句话说清**：VTracer 是一个可被 AI Agent 调用的高质量矢量描图引擎，ColorFlow 为其封装三种调用接口。

### 1.2 解决了什么问题

| 痛点 | 现状 | ColorFlow 解决后 |
|------|------|-----------------|
| AI 生成图片是位图，无法无损缩放 | 设计师手动描图，耗时 2-4 小时 | AI Agent 自动调用，分钟级输出 SVG |
| 不同工具接口不一，集成成本高 | 每接入一个工具要写一套适配代码 | 三种标准接口（API/SDK/CLI），一次接入 |
| 参数配置复杂，非专业用户难以上手 | 10+ 个参数，不知道怎么调 | AI Agent 负责参数决策，封装层只执行 |
| 批量处理能力弱 | 人工一张张处理 | API 支持批量，CLI 支持脚本自动化 |

### 1.3 目标用户

**直接用户（第一层）**：
- AI Agent 开发者（智能体、Bot、系统集成商）
- 需要将 AI 生成图片矢量化的技术团队

**间接用户（第二层）**：
- 使用 AI Agent 产品的最终客户（感受不到 ColorFlow 的存在）

---

## 二、产品定位与目标

### 2.1 战略定位

```
┌─────────────────────────────────────────────────────────┐
│                      AI Agent（外部）                     │
│           负责意图理解、参数决策、结果处理                  │
└─────────────────────┬───────────────────────────────────┘
                      │ 调用接口
                      ▼
┌─────────────────────────────────────────────────────────┐
│             ColorFlow 封装层（本项目 MVP）                │
│                                                         │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐                  │
│  │ API KEY │  │  SDK    │  │  CLI    │                  │
│  └─────────┘  └─────────┘  └─────────┘                  │
│                    ▼                                    │
│             VTracer Core                                 │
│        （位图 → SVG/DXF 矢量转换）                        │
└─────────────────────────────────────────────────────────┘
```

**核心原则**：做工具，不做智能体。专注执行层，不越界到决策层。

### 2.2 产品目标（SMART）

| 目标 | 描述 | 衡量指标 |
|------|------|---------|
| **接入目标** | MVP 阶段完成三种接口交付 | API / SDK / CLI 全部可用 |
| **性能目标** | 单张 1080p 图片处理时间 | ≤ 5 秒（本地）|
| **质量目标** | 转换后 SVG 路径完整度 | ≥ 95%（无明显断线）|
| **兼容目标** | 支持主流图片格式 | PNG / JPG / WebP / BMP |
| **文档目标** | 开发文档完整度 | 覆盖 100% 接口，含示例 |

### 2.3 MVP 边界

**MVP 做（Yes）**：
- VTracer Python SDK 封装
- FastAPI HTTP 接口（API KEY 认证）
- CLI 封装脚本
- 核心参数规格定义
- 错误处理与重试策略
- 接口文档（OpenAPI/Swagger）

**MVP 不做（No）**：
- AI Agent 意图理解模块
- 自然语言参数生成
- AI 结果校验/降级
- 用户管理/计费系统
- 矢量格式高级编辑
- 多语言国际化（i18n）

---

## 三、核心功能

### 3.1 功能矩阵

| 功能 | 类型 | 优先级 | 说明 |
|------|------|--------|------|
| Python SDK 封装 | 核心 | P0 | trace() / trace_bytes() |
| FastAPI HTTP 接口 | 核心 | P0 | /api/v1/trace |
| API KEY 认证 | 核心 | P0 | Header 注入验证 |
| CLI 封装 | 核心 | P0 | wrapper.sh 一键调用 |
| 参数校验 | 核心 | P0 | 类型/范围/合法性检查 |
| 错误处理 | 核心 | P0 | 400/401/404/500/503 |
| 重试策略 | 增强 | P1 | 自动降级 mode 重试 |
| 不落盘转换 | 增强 | P1 | trace_bytes() 内存模式 |
| Docker 镜像 | 增强 | P1 | CLI 容器化部署 |
| OpenAPI 文档 | 增强 | P2 | 自动生成 Swagger UI |

### 3.2 接口规格

#### 3.2.1 API KEY 接口（HTTP）

```
POST /api/v1/trace
Content-Type: multipart/form-data
Headers:
  X-API-KEY: <api_key>

Form Fields:
  - image: 文件（必需）
  - mode: string (可选, 默认 "color")
  - colormode: string (可选, 默认 "rgb8")
  - hierarchical: string (可选, 默认 "stacked")
  - filter_speckle: int (可选, 默认 4)
  - color_precision: int (可选, 默认 6)
  - layer_difference: int (可选, 默认 64)
  - corner_threshold: int (可选, 默认 60)
  - length_threshold: float (可选, 默认 2.0)
  - path_precision: int (可选, 默认 7)

Response:
  200: SVG 文件二进制
  400: 参数错误
  401: API KEY 无效
  404: 输入文件不存在
  500: VTracer 执行失败
  503: 服务过载
```

#### 3.2.2 Python SDK 接口

```python
from colorflow_sdk import ColorFlowSDK

# 初始化
sdk = ColorFlowSDK(output_dir="/tmp")

# 基本调用
svg_path = sdk.trace("input.png")

# 完整参数
svg_path = sdk.trace(
    "input.png",
    mode="color",
    filter_speckle=4,
    layer_difference=64,
    corner_threshold=60,
    path_precision=7,
)

# 字节模式（不落盘）
svg_bytes = sdk.trace_bytes(image_bytes, image_format="png")
```

#### 3.2.3 CLI 接口

```bash
# 基本用法
colorflow --input input.png --output output.svg

# 完整参数
colorflow \
  --input input.png \
  --output output.svg \
  --mode color \
  --filter-speckle 4 \
  --layer-difference 64 \
  --corner-threshold 60 \
  --path-precision 7
```

### 3.3 参数规格

| 参数 | 类型 | 默认值 | 可选值 | 说明 |
|------|------|--------|--------|------|
| mode | string | color | color/grey/human | 描图模式 |
| colormode | string | rgb8 | rgb8/rgb16/mono/grey/grey16 | 颜色模式 |
| hierarchical | string | stacked | flat/stacked | 输出层级 |
| filter_speckle | int | 4 | 1-100 | 斑点过滤，越大过滤越多 |
| color_precision | int | 6 | 1-16 | 颜色精度 |
| layer_difference | int | 64 | 1-256 | 图层距离阈值 |
| corner_threshold | int | 60 | 1-180 | 角点阈值 |
| length_threshold | float | 2.0 | 0.1-100 | 长度阈值 |
| path_precision | int | 7 | 1-16 | 路径精度 |

**mode 适用场景**：

| mode | 适用场景 |
|------|---------|
| color | 彩色包装效果图、Logo、插图 |
| grey | 灰度图、线条图、印刷稿 |
| human | 人像、人物照片（专项优化）|

---

## 四、用户故事与场景

### 4.1 核心用户故事

#### 故事 1：AI 绘图智能体调用
> 作为一个 AI 绘图智能体，我需要在用户上传图片时自动调用矢量描图功能，这样用户可以直接获得 SVG 文件用于印刷或二次编辑。

**验收标准**：
- [ ] 智能体调用 SDK 接口，传入图片路径和 mode 参数
- [ ] 3 秒内返回 SVG 文件路径
- [ ] 错误时自动降级并记录日志

#### 故事 2：批量处理设计素材
> 作为一个设计师，我需要批量将 100 张产品图转为 SVG，这样我可以快速建档而不必一张张手动处理。

**验收标准**：
- [ ] CLI 支持 shell 循环批量处理
- [ ] 批量处理时 CPU 利用率 ≤ 80%
- [ ] 支持断点续传（跳过已处理文件）

#### 故事 3：远程 API 调用
> 作为一个无头客户端（Headless Client），我需要在没有 Python 环境的情况下通过 HTTP 调用矢量描图服务。

**验收标准**：
- [ ] HTTP 接口返回标准 HTTP 状态码
- [ ] API KEY 认证生效，无效 KEY 返回 401
- [ ] 请求超时时间 ≤ 30 秒

### 4.2 典型使用场景

| 场景 | 调用方式 | 输入 | 输出 |
|------|---------|------|------|
| AI 智能体自动处理 | Python SDK | 用户上传图片 | SVG 文件 |
| 设计师批量转换 | CLI + 脚本 | 100 张 PNG | 100 个 SVG |
| H5 在线转换工具 | HTTP API | Base64 图片 | SVG 数据 |
| 无头服务器部署 | Docker CLI | 挂载目录图片 | 挂载目录 SVG |

---

## 五、产品路线图

### 5.1 MVP 阶段（Week 1-4）

**目标**：三种接口可用，文档完整，可交付 AI Agent 集成。

```
Week 1          Week 2          Week 3          Week 4
├─ SDK 封装      ├─ API 服务      ├─ CLI 封装      ├─ 文档完善
│  ├─ trace()   │  ├─ FastAPI    │  ├─ wrapper    │  ├─ README
│  ├─ trace_bytes│ │  ├─ /trace  │  ├─ Docker    │  ├─ API 文档
│  └─ 参数校验   │  └─ API KEY   │  └─ 测试用例   │  └─ 部署指南
```

**交付物**：
- [ ] Python SDK（pip install）
- [ ] FastAPI 服务（uvicorn 启动）
- [ ] CLI 工具（Docker 镜像）
- [ ] 完整开发文档

### 5.2 后续迭代方向（Post-MVP）

| 阶段 | 功能 | 优先级 | 说明 |
|------|------|--------|------|
| **v1.1** | 批量 API | P1 | 支持一次请求多张图片 |
| **v1.2** | WebSocket | P1 | 实时进度推送 |
| **v1.3** | DXF 格式 | P2 | 支持 CAD 格式输出 |
| **v2.0** | 用户系统 | P2 | 多租户/计费/配额 |
| **v2.0** | 参数智能推荐 | P2 | 根据图片内容推荐参数 |

---

## 六、里程碑与 KPI

### 6.1 MVP 里程碑

| 里程碑 | 计划日期 | 交付内容 | 验收标准 |
|--------|---------|---------|---------|
| M1: SDK Ready | Week 1 末 | Python SDK 封装 | trace() / trace_bytes() 可用，单元测试覆盖 ≥ 80% |
| M2: API Ready | Week 2 末 | FastAPI 服务 | /api/v1/trace 可调，API KEY 认证生效 |
| M3: CLI Ready | Week 3 末 | CLI + Docker | vtracer 命令可用，Docker 镜像 < 200MB |
| M4: MVP Ship | Week 4 末 | 完整交付 | 文档完整，可交付给 AI Agent 团队 |

### 6.2 KPI 定义

| KPI | 定义 | 目标值 | 测量方式 |
|-----|------|--------|---------|
| **接口可用率** | 三种接口均可正常调用 | 100% | 集成测试 |
| **错误率** | 有效请求中返回 5xx 的比例 | ≤ 1% | 日志统计 |
| **处理时长 P95** | 1080p 图片处理时间 P95 分位 | ≤ 5 秒 | 性能测试 |
| **文档覆盖率** | 接口有文档说明的比例 | 100% | 人工评审 |
| **Bug 密度** | 千行代码 Bug 数 | ≤ 5 | 缺陷统计 |

---

## 七、质量管理

### 7.1 测试策略

```
┌─────────────────────────────────────────┐
│              测试金字塔                   │
│                                         │
│            ┌───────────┐                │
│            │  端到端测试 │  ← 真实调用验证  │
│           ┌┴───────────┴┐               │
│           │   集成测试    │  ← API/SDK    │
│          ┌┴─────────────┴┐              │
│          │    单元测试     │  ← 函数级    │
│          └────────────────┘              │
└─────────────────────────────────────────┘
```

**单元测试**：
- 参数校验逻辑
- 错误处理分支
- SDK trace / trace_bytes 正常路径

**集成测试**：
- API 接口端到端（发送请求→返回 SVG）
- CLI 命令执行
- Docker 容器运行

**测试用例优先级**：

| 用例 | 优先级 | 说明 |
|------|--------|------|
| trace() 正常返回 SVG | P0 | 核心路径 |
| trace_bytes() 内存模式 | P0 | 核心路径 |
| API KEY 错误返回 401 | P0 | 安全验证 |
| 无效参数返回 400 | P0 | 参数校验 |
| CLI 基本调用 | P1 | 交付验收 |
| Docker 容器运行 | P1 | 交付验收 |

### 7.2 错误处理规范

| 错误码 | 类型 | 定义 | AI Agent 处理建议 |
|--------|------|------|-----------------|
| 400 | 参数错误 | 输入参数不合法 | 检查参数范围和类型 |
| 401 | 认证失败 | API KEY 无效或缺失 | 检查 KEY 是否正确 |
| 404 | 文件不存在 | 输入图片路径无效 | 检查文件路径 |
| 500 | 执行失败 | VTracer 内部错误 | 重试（最多 3 次）|
| 503 | 服务过载 | 并发超限或资源不足 | 排队重试 |

**重试策略**：

```python
def trace_with_retry(sdk, image_path, max_retries=3, **kwargs):
    """带降级重试的 VTracer 调用"""
    last_error = None
    modes = ["color", "grey", "human"]  # 按优先级降级

    for mode in modes[:max_retries]:
        try:
            return sdk.trace(image_path, mode=mode, **kwargs)
        except Exception as e:
            last_error = e
            continue

    raise RuntimeError(f"VTracer failed after {max_retries} retries: {last_error}")
```

---

## 八、非功能需求

### 8.1 性能需求

| 指标 | 目标 | 说明 |
|------|------|------|
| 处理时长（1080p） | ≤ 5 秒 | 本地 VTracer 执行时间 |
| 并发能力 | ≥ 10 QPS | API 服务单实例 |
| 内存占用 | ≤ 512MB | 单次请求峰值 |
| 启动时间 | ≤ 3 秒 | Docker 冷启动 |

### 8.2 兼容性需求

| 项目 | 要求 |
|------|------|
| Python 版本 | 3.10 / 3.11 / 3.12 |
| 操作系统 | Linux / macOS / Windows（WSL）|
| Docker | 20.10+ |
| 输入格式 | PNG / JPG / WebP / BMP |
| 输出格式 | SVG（DXF 规划中）|

### 8.3 安全需求

| 项目 | 要求 |
|------|------|
| API KEY | 必须使用 HTTPS 传输（生产环境）|
| 文件上传 | 仅允许图片格式，拒绝可执行文件 |
| 路径遍历 | 禁止 ../ 路径穿透 |
| 敏感信息 | 日志中禁止打印 API KEY |

---

## 九、附录

### 9.1 术语表

| 术语 | 定义 |
|------|------|
| VTracer | Rust 编写的矢量描图库，ColorFlow 的核心执行引擎 |
| AI Native | 专为 AI 工作流设计的接口和交互模式 |
| SDK | Software Development Kit，软件开发包 |
| API KEY | 接口访问密钥，用于身份认证 |
| 矢量描图 | 将位图转换为矢量路径的技术 |
| trace | 追踪/描图，本项目中指位图转矢量的过程 |

### 9.2 参考资料

- VTracer 官方仓库：https://github.com/Elringus/vtracer
- FastAPI 官方文档：https://fastapi.tiangolo.com
- 飞书文档：https://feishu.cn/docx/Z4xWdsvSToJ0O2xNZQWcHtT5nPf

---

*文档作者：法师 | 最后更新：2026-05-09*
